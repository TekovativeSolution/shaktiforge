<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="quality_control_line_tree" model="ir.ui.view">
        <field name="name">quality.control.line.tree.view</field>
        <field name="model">quality.control.line</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="test_type_id"/>
                <field name="norm"/>
                <field name="norm_unit"/>
                <field name="tolerance_min"/>
                <field name="tolerance_max"/>
            </tree>
        </field>
    </record>

    <record id="quality_control_line_form" model="ir.ui.view">
        <field name="name">quality.control.line.form.view</field>
        <field name="model">quality.control.line</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                <field name="test_type_id"/>
                            <field name="test_type" invisible="1"/>
                <label for="norm" invisible="test_type != 'measure'"/>
                <div class="o_row" invisible="test_type != 'measure'">
                    <field name="norm" required="test_type == 'measure'"/>
                    <field name="norm_unit" string="Unit of Measure" required="test_type == 'measure'"/>
                </div>
                <label for="tolerance_min" string="Tolerance" invisible="test_type != 'measure'"/>
                <div invisible="test_type != 'measure'" class="o_row">
                    <span>from </span> <field name="tolerance_min"/>
                    <span>to </span> <field name="tolerance_max"/>
                </div>
                    </group>
                        <group>
                    </group>
                    </group>
                    <notebook>
                            <page  string="Instruction" name="instruction">
                                <field name="instruction"/>
                            </page>
                        <page  string="Notes" name="notes">
                                <field name="note"/>
                            </page>
                        <page  string="Message if Failure" name="failure_message"  invisible="test_type in ['picture', 'instructions', 'register_consumed_materials', 'print_label']">
                                <field name="failure_message"/>
                            </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

</odoo>